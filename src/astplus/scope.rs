pub struct Scope {}

impl Scope {
    pub fn new() -> Scope {
        Scope {}
    }
}

#[cfg(test)]
mod tests {

    // Note this useful idiom: importing names from outer (for mod tests) scope.
    use super::*;

}